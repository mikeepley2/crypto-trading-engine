apiVersion: apps/v1
kind: Deployment
metadata:
  name: momentum-hype-detector
  namespace: crypto-trading
  labels:
    app: momentum-hype-detector
    component: signal-generation
    service-type: momentum-analysis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: momentum-hype-detector
  template:
    metadata:
      labels:
        app: momentum-hype-detector
        component: signal-generation
        service-type: momentum-analysis
    spec:
      containers:
      - name: momentum-hype-detector
        image: megabob70/crypto-momentum-hype-detector:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8029
          name: http
        env:
        - name: DB_HOST
          value: "host.docker.internal"
        - name: DB_USER
          value: "news_collector"
        - name: DB_PASSWORD
          value: "99Rules!"
        - name: DB_NAME
          value: "crypto_prices"
        - name: PYTHONUNBUFFERED
          value: "1"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8029
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8029
          initialDelaySeconds: 5
          periodSeconds: 10
        startupProbe:
          httpGet:
            path: /health
            port: 8029
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 12
      restartPolicy: Always